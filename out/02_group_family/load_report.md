# Load report — 02_group_family

Cited known_issues.md ids: D2, D1, D8, D9, K7
Cited but not loader-actionable (no fix block): D1, D8, D9, K7
## group_started (created)
- rows loaded: 1200
- normalized `application_id` per D2
- D2 verify on `application_id`: overlap_pct = 0.0% -> STOP -- report as finding, analyse table standalone only

## traveller_added (created)
- rows loaded: 3495
- normalized `application_id` per D2
- D2 verify on `application_id`: overlap_pct = 0.0% -> STOP -- report as finding, analyse table standalone only

## traveller_removed (created)
- rows loaded: 70
- normalized `application_id` per D2
- D2 verify on `application_id`: overlap_pct = 0.0% -> STOP -- report as finding, analyse table standalone only

## group_submitted (created)
- rows loaded: 688
- normalized `application_id` per D2
- D2 verify on `application_id`: overlap_pct = 0.0% -> STOP -- report as finding, analyse table standalone only
